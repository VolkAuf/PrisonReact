export interface GamePreview {
  name: string;
  gameType: string;
  shortDescription: string;
  icon: string;
}

export interface GameDescription {
  title: string;
  description: string;
  icon: string;
}
